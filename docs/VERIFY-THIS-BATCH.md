# 漓江回声 · 本批验收指南(战斗视觉 + 描绘 + 谱面)

> 分支 `stage-start-authoring`。先 `git pull` → 等编译 → Console 无红报错。
> 用 **`漓江回声→调试`** 菜单一键进各阶段测(战斗只在 LijiangEchoMR_Main 里跑)。

---

## A. 战斗音符(菜单 调试→进 战斗(关卡1))
- [ ] **单击=鱼纹**(select/fish_symbol);长按=蛇纹;双击=鸟纹
- [ ] 音符**朝正中心飞、落点居中**(右侧音符不再偏左)——若还偏,是鱼纹图内容不居中,告诉我加偏移
- [ ] 打击纹样**纯白色**(渐显到全亮,进环略淡但清晰)
- [ ] **明显金色发光光晕**围绕音符一圈、脉动(放大 1.55 倍)——不够明显→告诉我再加强/换成亮圆环
- [ ] 长按音符按住时纹样**龙头→龙尾逐步消失**
- 可调常量:`NoteSizeScale`(大小)、光晕 `1.55f` 与颜色 `(1,0.95,0.55)`、`HoldWipeTowardEntrySide`(长按方向)

## B. 过场地平线远山(菜单 调试→进 过场(关卡1))
- [ ] 远方地平线一排**小远山**(约原 1/5、底面贴地平线、横向排开、静止不动)
- 位置/高低不对 → 调 ShowIntro 里 `horizonY`(地平线高度)/`mtnHeight`(山高)/`horizonMtnX`(各山横坐标)

## C. 描绘 · 双手镜像(菜单 调试→进 描绘(关卡1))
- [ ] 描绘时**指引线/已描绘线/光标都镜像到对侧**,左右对称(双手画效果)
- [ ] 已画线仍有头→尾发光渐变
- 想切回**单手画全程**:把代码里 `ExternalTraceMirror ?? true` 的 `true` 改成 `false`(或运行时设 `ExternalTraceMirror=false`)
- 注:这是镜像"渲染"版(视觉对称);要"真双手各画一半+各自判定"另议

## D. 结算(菜单 调试→进 结算(关卡1))
- [ ] 战斗结束 → **纹样解析卡片(可翻页)** → 按键/10秒 → **结算**
      （这是项目原设计,我之前误改已撤回、恢复原样)
- 若你确实想去掉那张卡、战斗完直接结算 → 明确说,我再改(属设计改动)

## E. 谱面从音乐生成(核心,菜单 漓江回声→谱面)
前置:选 `LijiangEchoAudio/battle_music` → Inspector **Load Type=Decompress On Load** + 勾 Preload Audio Data → **Apply**
- [ ] 菜单 `1. 从音乐检测拍子生成谱面` → 弹"检测到 N 个拍子点",生成 chart_generated.txt(点多/少调 `Sensitivity`/`MinGapSeconds`)
- [ ] 菜单 `2. 把需求类型贴到最近拍子`
- [ ] 进战斗 → Console 打印 **"已从谱面表格加载 N 个音符"** = 用上你音乐生成的谱面
      （没生成时默认用需求表 chart_liusanjie 的 33 音符)

## F. ① Stage_Start 场景化(你已烘焙提交,复核)
- [ ] 打开 `Stage_Start.unity`,`开始舞台` 下 20 个子物体在;`场景化→3.校验` 仍绿灯
- [ ] Bootstrap Play 开始界面正常、不重复叠加

---

## 待你回一句
- [ ] 双击贴图 鸟纹/蛙纹 · [ ] 右下角静止蛇纹(待描绘纹样)留/删/挪 · [ ] 中心圆环也做 Prefab? · [ ] 每场景单开加测试相机?
- [ ] 结算那张纹样卡:保留(原设计)还是去掉直接结算?

## 只能你/硬件
- [ ] Prefab 化 · Quest 3 真机测试 · 最终录制

---
*任一步不对:贴 Console 报错/现象/截图给 Claude,当条 `git revert` 或直接修。*
